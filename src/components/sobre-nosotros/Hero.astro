<header class="header-container">
  <article class="image-container">
    <aside class="header-img">
      <h2
        class="image-title animate__animated animate__fadeIn animate__delay-2s animate__slow">
        Sobre Nosotros
      </h2>
    </aside>
  </article>
  <article class="text-container animate__animated animate__slideInRight">
    <h1 class="hero-title">GRUPO MOKA</h1>

    <div>
      <h2 class="title">Odontología por especialistas</h2>
      <h3 class="subtitle">Consultorios y formación profesional</h3>
      <aside class="circles-container">
        <div class="circle"></div>
        <div class="circle"></div>
        <div class="circle"></div>
        <div class="circle"></div>
      </aside>
    </div>
  </article>
</header>

<style>
  .header-container {
    display: flex;
    align-items: center;
    height: 100vh;
    width: 100%;
    background: linear-gradient(90deg, #1a13388c, var(--White));
  }

  .image-container {
    width: 50%;
    height: 100vh;
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .image-title {
    color: var(--White);
    border-bottom: 3px solid var(--White);
    padding-bottom: 0.5rem;
  }

  .header-img {
    width: 100%;
    height: 100vh;
    clip-path: polygon(0% 0%, 75% 0%, 100% 50%, 75% 100%, 0% 100%);
    background-image: url("../../assets/images/sobrenosotros.png");
    background-size: cover;
    background-position: left;
    background-repeat: no-repeat;
    display: flex;
    justify-content: center;
    align-items: flex-end;
    flex-direction: column;
    padding-right: 4rem;
    gap: 1rem;
  }

  .text-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    width: 50%;
    padding: 2rem;
    margin-top: 4rem;
  }

  .hero-title {
    font-family: "Acumin";
    font-size: 4rem;
    font-weight: 100;
    color: var(--Darkerblue);
    text-align: center;
  }

  .title {
    font-family: "Acumin";
    font-size: 1.5rem;
    letter-spacing: 2px;
    color: var(--Darkerblue);
    margin-bottom: 1rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--Gray);
    text-align: center;
  }
  .subtitle {
    text-transform: uppercase;
    font-family: "ModernSans";
    color: var(--Darkblue);
    font-size: 1rem;
  }
  .circles-container {
    margin-top: 1.5rem;

    display: flex;
    justify-content: space-evenly;
    align-items: center;
    gap: 5rem;
  }
  .circle {
    width: 15px;
    height: 15px;
    border-radius: 50%;
  }

  .circle:first-child {
    background-color: var(--Darkerblue);
  }

  .circle:nth-child(2) {
    background-color: var(--Darkblue);
  }

  .circle:nth-child(3) {
    background-color: var(--Mediumblue);
  }

  .circle:last-child {
    background-color: var(--Lightblue);
  }
</style>
